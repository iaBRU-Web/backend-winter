services:
  - type: web
    name: winter-ai
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /api/v1/health
    autoDeploy: true
